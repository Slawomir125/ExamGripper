CREATE TABLE IF NOT EXISTS pytania_inf04 ( pytanie_id INT PRIMARY KEY AUTO_INCREMENT, pytanie TEXT NOT NULL, odpowiedzi_dobra_kolejnosc JSON NOT NULL ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_polish_ci;
INSERT INTO pytania_inf04 (pytanie, odpowiedzi_dobra_kolejnosc) VALUES ('Używając podanych elementów, uzupełnij kod funkcji obliczającej silnię rekurencyjnie w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n        return n * silnia(n - 1)\n    if n <= 1:\ndef silnia(n):\n        return 1\n    else:', '["def silnia(n):", "    if n <= 1:", "        return 1", "    else:", "        return n * silnia(n - 1)"]'), ('Używając podanych elementów, uzupełnij kod funkcji obliczającej ciąg Fibonacciego iteracyjnie w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n    return b\n    for i in range(2, n + 1):\n    a, b = 0, 1\n    if n <= 1:\ndef fib(n):\n        a, b = b, a + b\n        return n', '["def fib(n):", "    if n <= 1:", "        return n", "    a, b = 0, 1", "    for i in range(2, n + 1):", "        a, b = b, a + b", "    return b"]'), ('Używając podanych elementów, uzupełnij kod funkcji sortującej tablicę metodą bąbelkową w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n    for i in range(n):\n    n = len(arr)\ndef bubble_sort(arr):\n        for j in range(0, n - i - 1):\n            if arr[j] > arr[j + 1]:', '["def bubble_sort(arr):", "    n = len(arr)", "    for i in range(n):", "        for j in range(0, n - i - 1):", "            if arr[j] > arr[j + 1]:", "                arr[j], arr[j + 1] = arr[j + 1], arr[j]", "    return arr"]'), ('Używając podanych elementów, uzupełnij kod funkcji sprawdzającej czy napis jest palindromem w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n    return s == s[::-1]\ndef is_palindrome(s):\n    s = s.lower().replace(" ", "")\n    if not s:\n        return True', '["def is_palindrome(s):", "    s = s.lower().replace(" ", "")", "    if not s:", "        return True", "    return s == s[::-1]"]'), ('Używając podanych elementów, uzupełnij kod funkcji znajdującej największy element w liście w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n    if not arr:\n        return None\n    max_val = arr[0]\ndef find_max(arr):\n        for x in arr:\n            if x > max_val:\n                max_val = x\n    return max_val', '["def find_max(arr):", "    if not arr:", "        return None", "    max_val = arr[0]", "    for x in arr:", "        if x > max_val:", "            max_val = x", "    return max_val"]'), ('Używając podanych elementów, uzupełnij kod funkcji obliczającej sumę elementów tablicy w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n    suma = 0\ndef sum_array(arr):\n        for x in arr:\n            suma += x\n    return suma', '["def sum_array(arr):", "    suma = 0", "    for x in arr:", "        suma += x", "    return suma"]'), ('Używając podanych elementów, uzupełnij kod funkcji sprawdzającej czy liczba jest pierwsza w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n        return False\n    for i in range(2, int(n0.5) + 1):\n    if n <= 1:\ndef is_prime(n):\n            if n % i == 0:\n                return True', '["def is_prime(n):", "    if n <= 1:", "        return False", "    for i in range(2, int(n0.5) + 1):", "        if n % i == 0:", "            return False", "    return True"]'), ('Używając podanych elementów, uzupełnij kod funkcji odwracającej napis w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n    return s[::-1]\ndef reverse_string(s):\n    if not s:\n        return ""', '["def reverse_string(s):", "    if not s:", "        return """, "    return s[::-1]"]'), ('Używając podanych elementów, uzupełnij kod funkcji obliczającej średnią z listy liczb w języku Python, aby działał poprawnie.\n\nPodane fragmenty kodu:\n    return suma / len(arr)\n    suma = 0\ndef average(arr):\n        for x in arr:\n            suma += x\n    if not arr:', '["def average(arr):", "    if not arr:", "        return 0", "    suma = 0", "    for x in arr:", "        suma += x", "    return suma / len(arr)"]'), ('Używając podanych elementów, uzupełnij kod funkcji obliczającej silnię iteracyjnie w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n        long result = 1;\n    for (int i = 2; i <= n; i++) {\npublic static long silnia(int n) {\n        return result;\n    if (n <= 1) return 1;', '["public static long silnia(int n) {", "    if (n <= 1) return 1;", "    long result = 1;", "    for (int i = 2; i <= n; i++) {", "        result *= i;", "    }", "    return result;"]'), ('Używając podanych elementów, uzupełnij kod funkcji obliczającej n-ty element Fibonacciego w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    if (n <= 1) return n;\npublic static int fib(int n) {\n    int a = 0, b = 1;\n        int temp = a + b;\n    for (int i = 2; i <= n; i++) {\n        return b;', '["public static int fib(int n) {", "    if (n <= 1) return n;", "    int a = 0, b = 1;", "    for (int i = 2; i <= n; i++) {", "        int temp = a + b;", "        a = b;", "        b = temp;", "    }", "    return b;"]'), ('Używając podanych elementów, uzupełnij kod funkcji sortującej tablicę metodą bąbelkową w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n                int temp = arr[j];\n    for (int i = 0; i < n - 1; i++) {\npublic static void bubbleSort(int[] arr) {\n        for (int j = 0; j < n - i - 1; j++) {\n    int n = arr.length;\n            if (arr[j] > arr[j + 1]) {', '["public static void bubbleSort(int[] arr) {", "    int n = arr.length;", "    for (int i = 0; i < n - 1; i++) {", "        for (int j = 0; j < n - i - 1; j++) {", "            if (arr[j] > arr[j + 1]) {", "                int temp = arr[j];", "                arr[j] = arr[j + 1];", "                arr[j + 1] = temp;", "            }", "        }", "    }", "}"]'), ('Używając podanych elementów, uzupełnij kod funkcji sprawdzającej palindrom w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    return true;\npublic static boolean isPalindrome(String s) {\n    s = s.toLowerCase().replaceAll("\\s+", "");\n    int left = 0, right = s.length() - 1;\n        if (s.charAt(left) != s.charAt(right)) return false;\n    while (left < right) {', '["public static boolean isPalindrome(String s) {", "    s = s.toLowerCase().replaceAll("\\s+", "");", "    int left = 0, right = s.length() - 1;", "    while (left < right) {", "        if (s.charAt(left) != s.charAt(right)) return false;", "        left++;", "        right--;", "    }", "    return true;", "}"]'), ('Używając podanych elementów, uzupełnij kod funkcji znajdującej maksimum w tablicy w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    int max = arr[0];\npublic static int findMax(int[] arr) {\n    if (arr.length == 0) return Integer.MIN_VALUE;\n        if (arr[i] > max) max = arr[i];\n    for (int i = 1; i < arr.length; i++) {', '["public static int findMax(int[] arr) {", "    if (arr.length == 0) return Integer.MIN_VALUE;", "    int max = arr[0];", "    for (int i = 1; i < arr.length; i++) {", "        if (arr[i] > max) max = arr[i];", "    }", "    return max;", "}"]'), ('Używając podanych elementów, uzupełnij kod funkcji sumującej elementy tablicy w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    int sum = 0;\npublic static int sumArray(int[] arr) {\n        sum += arr[i];\n    for (int i = 0; i < arr.length; i++) {\n    return sum;', '["public static int sumArray(int[] arr) {", "    int sum = 0;", "    for (int i = 0; i < arr.length; i++) {", "        sum += arr[i];", "    }", "    return sum;", "}"]'), ('Używając podanych elementów, uzupełnij kod funkcji sprawdzającej czy liczba jest pierwsza w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n        return false;\n    for (int i = 2; i <= Math.sqrt(n); i++) {\npublic static boolean isPrime(int n) {\n    if (n <= 1) return false;\n            if (n % i == 0) return false;\n    return true;', '["public static boolean isPrime(int n) {", "    if (n <= 1) return false;", "    for (int i = 2; i <= Math.sqrt(n); i++) {", "        if (n % i == 0) return false;", "    }", "    return true;", "}"]'), ('Używając podanych elementów, uzupełnij kod funkcji odwracającej tablicę w języku Java (konsola), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    for (int i = 0; i < n / 2; i++) {\npublic static void reverseArray(int[] arr) {\n    int n = arr.length;\n        int temp = arr[i];\n        arr[n - 1 - i] = temp;\n    return;', '["public static void reverseArray(int[] arr) {", "    int n = arr.length;", "    for (int i = 0; i < n / 2; i++) {", "        int temp = arr[i];", "        arr[i] = arr[n - 1 - i];", "        arr[n - 1 - i] = temp;", "    }", "}"]'), ('Używając podanych elementów, uzupełnij kod metody onCreate w klasie Activity aplikacji mobilnej Android (Java), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    setContentView(R.layout.activity_main);\nprotected void onCreate(Bundle savedInstanceState) {\n    super.onCreate(savedInstanceState);\n    Button btn = findViewById(R.id.button);\n    btn.setOnClickListener(v -> textView.setText("Kliknięto!"));\n}', '["protected void onCreate(Bundle savedInstanceState) {", "    super.onCreate(savedInstanceState);", "    setContentView(R.layout.activity_main);", "    Button btn = findViewById(R.id.button);", "    btn.setOnClickListener(v -> textView.setText("Kliknięto!"));", "}"]'), ('Używając podanych elementów, uzupełnij kod obsługi zdarzenia przycisku w aplikacji mobilnej Android (Java), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    button.setOnClickListener(new View.OnClickListener() {\npublic void onClick(View v) {\n        count++;\n        textView.setText(String.valueOf(count));\n    });\n}', '["button.setOnClickListener(new View.OnClickListener() {", "    public void onClick(View v) {", "        count++;", "        textView.setText(String.valueOf(count));", "    }", "});"]'), ('Używając podanych elementów, uzupełnij kod metody startActivity z Intent w aplikacji mobilnej Android (Java), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    Intent intent = new Intent(this, SecondActivity.class);\npublic void openSecondActivity() {\n    startActivity(intent);\n}', '["public void openSecondActivity() {", "    Intent intent = new Intent(this, SecondActivity.class);", "    startActivity(intent);", "}"]'), ('Używając podanych elementów, uzupełnij kod zapisu do SharedPreferences w aplikacji mobilnej Android (Java), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    SharedPreferences.Editor editor = prefs.edit();\npublic void saveData(String key, String value) {\n    editor.putString(key, value);\n    editor.apply();\n}', '["public void saveData(String key, String value) {", "    SharedPreferences prefs = getSharedPreferences("MyPrefs", MODE_PRIVATE);", "    SharedPreferences.Editor editor = prefs.edit();", "    editor.putString(key, value);", "    editor.apply();", "}"]'), ('Używając podanych elementów, uzupełnij kod listy RecyclerView w aplikacji mobilnej Android (Java), aby działał poprawnie.\n\nPodane fragmenty kodu:\n    recyclerView.setLayoutManager(new LinearLayoutManager(this));\npublic void setupRecyclerView() {\n    recyclerView.setAdapter(adapter);\n}', '["public void setupRecyclerView() {", "    recyclerView.setLayoutManager(new LinearLayoutManager(this));", "    recyclerView.setAdapter(adapter);", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu licznika w React Native (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const [count, setCount] = useState(0);\nfunction Counter() {\n  return (\n    \n      Licznik: {count}\n      <Button title="Zwiększ" onPress={() => setCount(count + 1)} />\n    \n  );\n}', '["function Counter() {", "  const [count, setCount] = useState(0);", "  return (", "    ", "      Licznik: {count}", "      <Button title="Zwiększ" onPress={() => setCount(count + 1)} />", "    ", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu wyświetlającego listę w React Native (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  return (\nfunction TodoList() {\n    <FlatList\n      data={todos}\n      renderItem={({item}) => {item}}\n    />\n  );\n}', '["function TodoList() {", "  const [todos] = useState(["Zadanie 1", "Zadanie 2"]);", "  return (", "    <FlatList", "      data={todos}", "      renderItem={({item}) => {item}}", "      keyExtractor={item => item}", "    />", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu z użyciem useEffect w React Native (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  useEffect(() => {\nfunction App() {\n    console.log("Komponent zamontowany");\n  }, []);\n  return Aplikacja mobilna;\n}', '["function App() {", "  useEffect(() => {", "    console.log("Komponent zamontowany");", "  }, []);", "  return Aplikacja mobilna;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z formularzem logowania w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const [email, setEmail] = useState('');\nfunction LoginForm() {\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    console.log("Logowanie...");\n  };\n  return (\n    \n      <input value={email} onChange={(e) => setEmail(e.target.value)} />\n      Zaloguj\n    \n  );\n}', '["function LoginForm() {", "  const [email, setEmail] = useState("");", "  const handleSubmit = (e) => {", "    e.preventDefault();", "    console.log("Logowanie...");", "  };", "  return (", "    ", "      <input value={email} onChange={(e) => setEmail(e.target.value)} />", "      <button type="submit">Zaloguj", "    ", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z listą zadań (ToDo) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const [todos, setTodos] = useState([]);\nfunction TodoApp() {\n  const addTodo = () => setTodos([...todos, "Nowe zadanie"]);\n  return (\n    \n      {todos.map(t => {t})}\n      Dodaj\n    \n  );\n}', '["function TodoApp() {", "  const [todos, setTodos] = useState([]);", "  const addTodo = () => setTodos([...todos, "Nowe zadanie"]);", "  return (", "    ", "      {todos.map(t => {t})}", "      Dodaj", "    ", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z użyciem fetch do API w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  useEffect(() => {\nfunction DataFetcher() {\n    fetch("https://api.example.com/data")\n      .then(res => res.json())\n      .then(data => setData(data));\n  }, []);\n  return {data};\n}', '["function DataFetcher() {", "  const [data, setData] = useState(null);", "  useEffect(() => {", "    fetch("https://api.example.com/data")", "      .then(res => res.json())", "      .then(data => setData(data));", "  }, []);", "  return {data};", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z routingiem (React Router) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  return (\nfunction App() {\n    \n      \n        <Route path="/" element={} />\n      \n    \n  );\n}', '["function App() {", "  return (", "    ", "      ", "        <Route path="/" element={} />", "      ", "    ", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z użyciem Context API w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nconst ThemeContext = createContext();\nfunction App() {\n  return (\n    <ThemeContext.Provider value="dark">\n      \n    </ThemeContext.Provider>\n  );\n}', '["const ThemeContext = createContext();", "function App() {", "  return (", "    <ThemeContext.Provider value="dark">", "      ", "    </ThemeContext.Provider>", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z reduktorem (useReducer) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const [state, dispatch] = useReducer(reducer, initialState);\nfunction CounterReducer() {\n  return (\n    <button onClick={() => dispatch({type: "increment"})}>Zwiększ\n  );\n}', '["function CounterReducer() {", "  const [state, dispatch] = useReducer(reducer, initialState);", "  return (", "    <button onClick={() => dispatch({type: "increment"})}>Zwiększ", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z memoizacją (useMemo) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const expensiveValue = useMemo(() => computeExpensiveValue(a, b), [a, b]);\nfunction ExpensiveComponent({a, b}) {\n  return Wartość: {expensiveValue};\n}', '["function ExpensiveComponent({a, b}) {", "  const expensiveValue = useMemo(() => computeExpensiveValue(a, b), [a, b]);", "  return Wartość: {expensiveValue};", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z ref (useRef) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const inputRef = useRef(null);\nfunction FocusInput() {\n  const focus = () => inputRef.current.focus();\n  return ;\n}', '["function FocusInput() {", "  const inputRef = useRef(null);", "  const focus = () => inputRef.current.focus();", "  return ;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z obsługą błędów (Error Boundary) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nclass ErrorBoundary extends React.Component {\n  componentDidCatch(error, info) {\n    this.setState({ hasError: true });\n  }\n  render() {\n    if (this.state.hasError) return Błąd!;\n    return this.props.children;\n  }\n}', '["class ErrorBoundary extends React.Component {", "  constructor(props) {", "    super(props);", "    this.state = { hasError: false };", "  }", "  componentDidCatch(error, info) {", "    this.setState({ hasError: true });", "  }", "  render() {", "    if (this.state.hasError) return Błąd!;", "    return this.props.children;", "  }", "}"]'), ('Używając podanych elementów, uzupełnij kod prostego serwera Node.js z Express w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\napp.get("/", (req, res) => res.send("Hello World"));\nconst express = require("express");\nconst app = express();\napp.listen(3000, () => console.log("Serwer działa"));', '["const express = require("express");", "const app = express();", "app.get("/", (req, res) => res.send("Hello World"));", "app.listen(3000, () => console.log("Serwer działa"));"]'), ('Używając podanych elementów, uzupełnij kod middleware w Node.js (JS) do logowania żądań w aplikacji webowej, aby działał poprawnie.\n\nPodane fragmenty kodu:\napp.use((req, res, next) => {\n  console.log(${req.method} \( {req.url});\n  next();\n});\n', '["app.use((req, res, next) => {", "  console.log( \){req.method} ${req.url});", "  next();", "});"]'), ('Używając podanych elementów, uzupełnij kod routingu REST API w Node.js (JS) w aplikacji webowej, aby działał poprawnie.\n\nPodane fragmenty kodu:\napp.post("/users", (req, res) => {\n  res.json({ id: 1, name: req.body.name });\n});\n', '["app.post("/users", (req, res) => {", "  res.json({ id: 1, name: req.body.name });", "});"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z obsługą stanu asynchronicznego w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const [loading, setLoading] = useState(true);\nfunction AsyncComponent() {\n  useEffect(() => {\n    fetchData().then(() => setLoading(false));\n  }, []);\n  if (loading) return Ładowanie...;\n}', '["function AsyncComponent() {", "  const [loading, setLoading] = useState(true);", "  useEffect(() => {", "    fetchData().then(() => setLoading(false));", "  }, []);", "  if (loading) return Ładowanie...;", "  return Gotowe;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z props drilling w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nfunction Parent() {\n  const value = "Dane";\n  return ;\n}\nfunction Child({value}) {\n  return {value};\n}', '["function Parent() {", "  const value = "Dane";", "  return ;", "}", "function Child({value}) {", "  return {value};", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z custom hook w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nfunction useCounter() {\n  const [count, setCount] = useState(0);\n  const increment = () => setCount(c => c + 1);\n  return {count, increment};\n}\nfunction Counter() {\n  const {count, increment} = useCounter();\n  return {count};\n}', '["function useCounter() {", "  const [count, setCount] = useState(0);", "  const increment = () => setCount(c => c + 1);", "  return {count, increment};", "}", "function Counter() {", "  const {count, increment} = useCounter();", "  return {count};", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z portalem (React Portal) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nfunction Modal({children}) {\n  return createPortal(children, document.getElementById("modal-root"));\n}\n', '["function Modal({children}) {", "  return createPortal(children, document.getElementById("modal-root"));", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z Suspense w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  <Suspense fallback={Ładowanie...}>\nfunction LazyApp() {\n    \n  \n}', '["function LazyApp() {", "  return (", "    <Suspense fallback={Ładowanie...}>", "      ", "    ", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod obsługi błędu w komponencie React w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  throw new Error("Błąd testowy");\nfunction BuggyComponent() {\n  return OK;\n}', '["function BuggyComponent() {", "  throw new Error("Błąd testowy");", "  return OK;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z animacją (framer-motion) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n<motion.div animate={{ scale: 1.2 }}>\nfunction AnimatedBox() {\n  return <motion.div animate={{ scale: 1.2 }}>Animacja</motion.div>;\n}', '["function AnimatedBox() {", "  return <motion.div animate={{ scale: 1.2 }}>Animacja</motion.div>;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z dark mode toggle w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const [theme, setTheme] = useState("light");\nfunction ThemeToggle() {\n  const toggle = () => setTheme(theme === "light" ? "dark" : "light");\n  return Przełącz;\n}', '["function ThemeToggle() {", "  const [theme, setTheme] = useState("light");", "  const toggle = () => setTheme(theme === "light" ? "dark" : "light");", "  return Przełącz;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z lokalizacją (i18n) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const { t } = useTranslation();\nfunction LocalizedComponent() {\n  return {t("hello")};\n}', '["function LocalizedComponent() {", "  const { t } = useTranslation();", "  return {t("hello")};", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z walidacją formularza w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  if (!email) return "Brak email";\nfunction FormValidator() {\n  const [email, setEmail] = useState("");\n  return <input onChange={e => setEmail(e.target.value)} />;\n}', '["function FormValidator() {", "  const [email, setEmail] = useState("");", "  const validate = () => {", "    if (!email) return "Brak email";", "  };", "  return <input onChange={e => setEmail(e.target.value)} />;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z drag and drop w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  onDrop={handleDrop}\nfunction DragDrop() {\n  return Przeciągnij;\n}', '["function DragDrop() {", "  return Przeciągnij;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z testowaniem (Jest) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\ntest("renderuje poprawnie", () => {\n  render();\n  expect(screen.getByText("Hello")).toBeInTheDocument();\n});\n', '["test("renderuje poprawnie", () => {", "  render();", "  expect(screen.getByText("Hello")).toBeInTheDocument();", "});"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z optymalizacją (React.memo) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nconst Memoized = React.memo(function Child({value}) {\n  return {value};\n});\n', '["const Memoized = React.memo(function Child({value}) {", "  return {value};", "});"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z hookiem useCallback w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  const memoizedCallback = useCallback(() => {\nfunction CallbackComponent() {\n    doSomething(a, b);\n  }, [a, b]);\n}', '["function CallbackComponent() {", "  const memoizedCallback = useCallback(() => {", "    doSomething(a, b);", "  }, [a, b]);", "  return Klik;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z lazy loading w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nconst LazyComponent = lazy(() => import("./LazyComponent"));\nfunction App() {\n  return <Suspense fallback={Ładowanie...}>;\n}', '["const LazyComponent = lazy(() => import("./LazyComponent"));", "function App() {", "  return <Suspense fallback={Ładowanie...}>;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z strict mode w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  <React.StrictMode>\nfunction Root() {\n    \n  </React.StrictMode>\n}', '["function Root() {", "  return (", "    <React.StrictMode>", "      ", "    </React.StrictMode>", "  );", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z concurrent mode (startTransition) w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  startTransition(() => setTab(tab));\nfunction TabSwitcher() {\n  const [tab, setTab] = useState(0);\n}', '["function TabSwitcher() {", "  const [tab, setTab] = useState(0);", "  const switchTab = () => startTransition(() => setTab(tab + 1));", "  return Przełącz;", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z server components w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\nasync function ServerComponent() {\n  const data = await fetchData();\n  return {data};\n}\n', '["async function ServerComponent() {", "  const data = await fetchData();", "  return {data};", "}"]'), ('Używając podanych elementów, uzupełnij kod komponentu React z action w aplikacji webowej (JS), aby działał poprawnie.\n\nPodane fragmenty kodu:\n  "use server";\nasync function createUser(formData) {\n  // logika\n}\n', '[""use server";", "async function createUser(formData) {", "  // logika", "}"]');
